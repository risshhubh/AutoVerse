services:
  - type: web
    name: autoverse
    env: static
    nodeVersion: "20"
    buildCommand: npm install --legacy-peer-deps && export NODE_OPTIONS=--max-old-space-size=4096 && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
