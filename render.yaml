services:
  - type: web
    name: autoverse
    env: static
    buildCommand: npm install && export NODE_OPTIONS=--max-old-space-size=4096 && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
